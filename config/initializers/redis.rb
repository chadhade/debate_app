# uri = URI.parse(ENV["REDIS_URL"])
# REDIS = Redis.new(:host => uri.host, :port => uri.port, :password => uri.password)
# Redis.connect(:url => ENV['REDIS_URL'])

# require File.expand_path('../../../listener', __FILE__)
# puts "Starting Listener"
# Listener.subscribe
