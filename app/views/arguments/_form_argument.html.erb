<%= form_for Argument.new, :remote => true do |f| %>
	<div class = "field">
		<%= f.text_area :content, :size => "36x5", :onkeyup => "textCounter(this, this.form.current_count, 1000);" %>
	</div>
	<%= f.hidden_field :debate_id, :value => debate.id %>
	<div class = "action">
		<%= f.submit "deBunk!" %>
	</div>	
<% end %>