<%= form_for current_debater.relationships.build(:followed_id => @debater.id), :remote => true do |f| %>
	<span><%= f.hidden_field :followed_id %></span>
	<span class="actions"><%= f.submit "Follow" %></span>
<% end %>