<% if debate.status[:status_code] <= 2 && debate.participant?(current_debater) && !current_debater.waiting_for%>
	<%= link_to image_tag("waiting.png", :title => "Alert Me When Debate Starts", :onclick => 'debater_waiting = true', :size => "50x50"), {:action => :waiting} %> 
<% end %>

